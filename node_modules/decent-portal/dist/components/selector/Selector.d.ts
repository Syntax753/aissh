import { ReactNode } from 'react';
import Direction from '../popOver/types/Direction';
type Props = {
    disabled?: boolean;
    label?: string;
    selectedOptionNo: number;
    optionNames: string[];
    displayAsTabs?: boolean;
    onChange?: (optionNo: number) => void;
    onClick?: (optionNo: number) => void;
    popoverContent?: ReactNode[];
    popoverPreferredDirection?: Direction | Direction[];
    popoverAllowedDirections?: Direction[];
    iconChars?: (string | null)[];
};
declare function Selector({ disabled, label, optionNames, onClick, onChange, selectedOptionNo, displayAsTabs, popoverContent, popoverPreferredDirection, popoverAllowedDirections, iconChars }: Props): import("react/jsx-runtime").JSX.Element;
export default Selector;
