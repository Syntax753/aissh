export type InitResults = {
    favIconUrl: string | null;
    isDecentBarEnabled: boolean;
};
export declare function init(appName: string, enabledDomains: string[], setModalDialogName: Function): Promise<InitResults>;
