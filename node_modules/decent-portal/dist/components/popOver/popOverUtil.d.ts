import { CSSProperties } from "react";
import Direction from "./types/Direction";
export declare const DEFAULT_DIRECTIONS: Direction[];
export declare function getTryDirections(preferredDirection?: Direction | Direction[], allowedDirections?: Direction[]): Direction[];
export declare function calcPopoverPositionStyle(tryDirections: Direction[], targetElement: HTMLDivElement, popoverContentElement: HTMLDivElement): CSSProperties;
