import { ReactNode } from 'react';
import Direction from './types/Direction';
type Props = {
    children: ReactNode;
    content: ReactNode;
    preferredDirection?: Direction | Direction[];
    allowedDirections?: Direction[];
};
declare function Popover({ children, content, preferredDirection, allowedDirections }: Props): import("react/jsx-runtime").JSX.Element;
export default Popover;
