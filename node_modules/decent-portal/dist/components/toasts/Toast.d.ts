export declare enum ToastType {
    INFO = "info",
    IMPORTANT = "important",
    ERROR = "error"
}
export declare enum ToastState {
    APPEARING = 0,
    VISIBLE = 1,
    DISAPPEARING = 2,
    GONE = 3
}
type Toast = {
    message: string;
    type: ToastType;
};
export default Toast;
